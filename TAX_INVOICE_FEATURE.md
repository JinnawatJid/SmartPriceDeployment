# ฟีเจอร์ค่าใบกำกับภาษี

## คำอธิบาย
เมื่อลูกค้าต้องการใบกำกับภาษี ระบบจะบวกค่าใบกำกับภาษี 10 บาท โดยแฝงเข้าไปในราคาต่อชิ้น

## วิธีการคำนวณ
1. นำ 10 บาท ÷ จำนวนสินค้าในตะกร้า = ค่าต่อชิ้น
2. ถ้าหารไม่ลงตัว ให้ปัดขึ้นให้ลง .50 หรือ .00 เท่านั้น

## ตัวอย่าง
- 3 ชิ้น: 10 ÷ 3 = 3.33... → ปัดขึ้นเป็น 3.50 บาท/ชิ้น
- 4 ชิ้น: 10 ÷ 4 = 2.50 → ลงตัว ใช้ 2.50 บาท/ชิ้น
- 5 ชิ้น: 10 ÷ 5 = 2.00 → ลงตัว ใช้ 2.00 บาท/ชิ้น
- 6 ชิ้น: 10 ÷ 6 = 1.67... → ปัดขึ้นเป็น 2.00 บาท/ชิ้น

## การเปลี่ยนแปลงในโค้ด

### Backend (pricing_router.py)
1. เพิ่ม `needTaxInvoice: bool = False` ใน `PricingRequest` model
2. เพิ่มฟังก์ชัน `calculate_tax_invoice_surcharge(item_count: int) -> float`
3. ในส่วน DEFAULT MODE: คำนวณค่าใบกำกับภาษีและบวกเข้าไปในราคา
4. ในส่วน NORMAL MODE: คำนวณค่าใบกำกับภาษีและบวกเข้าไปในราคา

### Frontend (Step6_Summary.jsx)
1. เพิ่ม `needTaxInvoice: state.needsTax ?? false` ในทั้ง 3 ตำแหน่งที่ส่ง pricing request

## ตำแหน่งการเปลี่ยนแปลง

### Backend
- `backend/pricing_router.py`:
  - บรรทัด 45-47: เพิ่ม `needTaxInvoice` ใน `PricingRequest`
  - บรรทัด 60-80: เพิ่มฟังก์ชัน `calculate_tax_invoice_surcharge`
  - บรรทัด 460-475: เพิ่มการคำนวณค่าใบกำกับภาษีในส่วน DEFAULT MODE
  - บรรทัด 985-1000: เพิ่มการคำนวณค่าใบกำกับภาษีในส่วน NORMAL MODE

### Frontend
- `frontend/src/pages/CreateQuote/Step6_Summary.jsx`:
  - บรรทัด 456: เพิ่ม `needTaxInvoice` ในตำแหน่งที่ 1
  - บรรทัด 586: เพิ่ม `needTaxInvoice` ในตำแหน่งที่ 2
  - บรรทัด 856: เพิ่ม `needTaxInvoice` ในตำแหน่งที่ 3

## การทำงาน
1. ลูกค้าเลือก "ต้องการใบกำกับภาษี" ใน TaxDeliverySection
2. ค่า `state.needsTax` ถูกตั้งเป็น `true`
3. เมื่อส่ง pricing request ไปยัง backend จะส่ง `needTaxInvoice: true`
4. Backend คำนวณค่าใบกำกับภาษีและบวกเข้าไปในราคาต่อชิ้น
5. ราคารวมจะรวมค่าใบกำกับภาษีแล้ว

## หมายเหตุ
- ค่าใบกำกับภาษีจะแฝงเข้าไปในราคาต่อชิ้น ไม่ใช่เป็นรายการแยก
- ค่าใบกำกับภาษีจะถูกคำนวณใหม่ทุกครั้งที่มีการเปลี่ยนแปลงจำนวนสินค้า
- ค่าใบกำกับภาษีจะถูกรวมในการคำนวณ VAT
