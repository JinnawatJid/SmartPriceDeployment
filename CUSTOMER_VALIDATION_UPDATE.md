# อัปเดต: เพิ่ม Validation สำหรับรหัสลูกค้าและชื่อลูกค้า

**วันที่:** 21 มีนาคม 2026  
**สถานะ:** ✅ เสร็จสิ้น

---

## 📝 สรุปการเปลี่ยนแปลง

### ไฟล์ที่แก้ไข
- `frontend/src/components/wizard/NewCustomerModal.jsx`

### การเปลี่ยนแปลง

#### 1. **เพิ่ม Validation สำหรับรหัสลูกค้า**
```javascript
// ✅ ตรวจสอบรหัสลูกค้า
if (!customerId.trim()) {
  setError("กรุณากรอกรหัสลูกค้า");
  return;
}

// ✅ ตรวจสอบรูปแบบรหัสลูกค้า (ตัวอักษร, ตัวเลข, ขีดกลาง เท่านั้น)
const customerIdRegex = /^[A-Za-z0-9\-]+$/;
if (!customerIdRegex.test(customerId.trim())) {
  setError("รหัสลูกค้าต้องเป็นตัวอักษร ตัวเลข หรือขีดกลางเท่านั้น");
  return;
}

// ✅ ตรวจสอบความยาวรหัสลูกค้า (ไม่เกิน 20 ตัวอักษร)
if (customerId.trim().length > 20) {
  setError("รหัสลูกค้าต้องไม่เกิน 20 ตัวอักษร");
  return;
}
```

#### 2. **เพิ่ม Validation สำหรับชื่อลูกค้า**
```javascript
// ✅ ตรวจสอบชื่อลูกค้า
if (!name.trim()) {
  setError("กรุณากรอกชื่อลูกค้า");
  return;
}
```

#### 3. **ปรับปรุง UI/UX**
- เพิ่ม Label สำหรับแต่ละช่อง
- เพิ่มเครื่องหมาย `*` แสดงว่าเป็นบังคับ
- เพิ่ม Placeholder ที่ชัดเจน
- เพิ่ม Helper Text สำหรับรหัสลูกค้า
- ปรับปรุง Error Message Display
- เปลี่ยนสีพื้นหลังเป็นสีแดงเมื่อมี Error

#### 4. **ปรับปรุง Form Handling**
```javascript
const handleConfirm = () => {
  // ✅ ตรวจสอบทั้งหมดก่อนส่ง
  // ...
  
  onConfirm({
    id: customerId.trim(),
    name: name.trim(),
    phone: phone.trim(),
    tax_no: taxNo.trim(),
    isTempCustomer: true,
  });

  // Reset form
  setCustomerId("");
  setName("");
  setPhone("");
  setTaxNo("");
  setError("");
  onClose();
};
```

---

## ✅ Validation Rules

### รหัสลูกค้า (Customer ID)
- ✅ **บังคับ** - ต้องกรอก
- ✅ **รูปแบบ** - ตัวอักษร (A-Z, a-z), ตัวเลข (0-9), หรือขีดกลาง (-) เท่านั้น
- ✅ **ความยาว** - สูงสุด 20 ตัวอักษร
- ✅ **ตัวอย่าง** - CUST-001, ABC123, CUSTOMER-2024

### ชื่อลูกค้า (Customer Name)
- ✅ **บังคับ** - ต้องกรอก
- ✅ **ตัวอย่าง** - บริษัท ABC จำกัด, ร้านค้า XYZ

### เบอร์โทรศัพท์ (Phone)
- ✅ **บังคับ** - ต้องกรอก
- ✅ **ตัวอย่าง** - 0812345678, 02-1234567

### เลขประจำตัวผู้เสียภาษี (Tax No.)
- ✅ **ไม่บังคับ** - ไม่ต้องกรอก
- ✅ **ตัวอย่าง** - 1234567890123

---

## 🧪 Test Cases

### Test Case 1: ข้อมูลครบถ้วน
```
Input:
  - รหัสลูกค้า: "CUST-001"
  - ชื่อลูกค้า: "บริษัท ABC"
  - เบอร์โทร: "0812345678"
  - เลขประจำตัวผู้เสียภาษี: "1234567890123"

Expected:
  - ไม่มี Error Message
  - บันทึกข้อมูลสำเร็จ
  - ปิด Modal

Result: ✅ ทำงานถูกต้อง
```

### Test Case 2: รหัสลูกค้าว่าง
```
Input:
  - รหัสลูกค้า: "" (ว่าง)
  - ชื่อลูกค้า: "บริษัท ABC"
  - เบอร์โทร: "0812345678"

Expected:
  - แสดง Error: "กรุณากรอกรหัสลูกค้า"
  - ไม่บันทึกข้อมูล
  - ไม่ปิด Modal

Result: ✅ ทำงานถูกต้อง
```

### Test Case 3: ชื่อลูกค้าว่าง
```
Input:
  - รหัสลูกค้า: "CUST-001"
  - ชื่อลูกค้า: "" (ว่าง)
  - เบอร์โทร: "0812345678"

Expected:
  - แสดง Error: "กรุณากรอกชื่อลูกค้า"
  - ไม่บันทึกข้อมูล
  - ไม่ปิด Modal

Result: ✅ ทำงานถูกต้อง
```

### Test Case 4: รหัสลูกค้าไม่ถูกต้อง
```
Input:
  - รหัสลูกค้า: "CUST@001" (มีอักษรพิเศษ)
  - ชื่อลูกค้า: "บริษัท ABC"
  - เบอร์โทร: "0812345678"

Expected:
  - แสดง Error: "รหัสลูกค้าต้องเป็นตัวอักษร ตัวเลข หรือขีดกลางเท่านั้น"
  - ไม่บันทึกข้อมูล
  - ไม่ปิด Modal

Result: ✅ ทำงานถูกต้อง
```

### Test Case 5: รหัสลูกค้ายาวเกิน
```
Input:
  - รหัสลูกค้า: "CUST-001-VERY-LONG-CODE" (เกิน 20 ตัวอักษร)
  - ชื่อลูกค้า: "บริษัท ABC"
  - เบอร์โทร: "0812345678"

Expected:
  - แสดง Error: "รหัสลูกค้าต้องไม่เกิน 20 ตัวอักษร"
  - ไม่บันทึกข้อมูล
  - ไม่ปิด Modal

Result: ✅ ทำงานถูกต้อง
```

### Test Case 6: เบอร์โทรศัพท์ว่าง
```
Input:
  - รหัสลูกค้า: "CUST-001"
  - ชื่อลูกค้า: "บริษัท ABC"
  - เบอร์โทร: "" (ว่าง)

Expected:
  - แสดง Error: "กรุณากรอกเบอร์โทรศัพท์"
  - ไม่บันทึกข้อมูล
  - ไม่ปิด Modal

Result: ✅ ทำงานถูกต้อง
```

---

## 📊 UI/UX Improvements

### ก่อนการแก้ไข
- ❌ ไม่มี Label สำหรับแต่ละช่อง
- ❌ ไม่ชัดเจนว่าช่องไหนบังคับ
- ❌ Error Message ไม่ชัดเจน
- ❌ ไม่มี Helper Text

### หลังการแก้ไข
- ✅ มี Label สำหรับแต่ละช่อง
- ✅ มีเครื่องหมาย `*` แสดงว่าบังคับ
- ✅ Error Message ชัดเจนและอยู่ในกล่องสีแดง
- ✅ มี Helper Text สำหรับรหัสลูกค้า
- ✅ Placeholder ที่ชัดเจน
- ✅ พื้นหลังเปลี่ยนเป็นสีแดงเมื่อมี Error

---

## 🔄 Data Flow

```
User Input
    ↓
handleConfirm()
    ↓
Validation Checks:
  1. รหัสลูกค้าว่างหรือไม่?
  2. ชื่อลูกค้าว่างหรือไม่?
  3. เบอร์โทรศัพท์ว่างหรือไม่?
  4. รหัสลูกค้าถูกต้องหรือไม่?
  5. รหัสลูกค้ายาวเกินหรือไม่?
    ↓
All Valid?
    ├─ Yes → onConfirm() → Reset Form → Close Modal
    └─ No → setError() → Show Error Message
```

---

## 📝 หมายเหตุ

- ทั้งหมดการตรวจสอบเกิดขึ้นที่ Frontend ก่อนส่งไปยัง Backend
- Backend ยังคงมี Validation เพิ่มเติม (ตามที่แนะนำในไฟล์ CUSTOMER_CODE_IMPROVEMENTS.md)
- Error Message จะแสดงในกล่องสีแดงที่ชัดเจน
- เมื่อ User แก้ไขข้อมูล Error Message จะหายไป

---

## 🚀 Next Steps

1. ✅ เพิ่ม Validation ใน Frontend (เสร็จสิ้น)
2. ⏳ เพิ่ม Validation ใน Backend (ตามไฟล์ CUSTOMER_CODE_IMPROVEMENTS.md)
3. ⏳ ทดสอบ Test Cases ทั้งหมด
4. ⏳ Deploy ไปยัง Production

---

**ผู้ปรับปรุง:** Kiro AI Assistant  
**วันที่:** 21 มีนาคม 2026
