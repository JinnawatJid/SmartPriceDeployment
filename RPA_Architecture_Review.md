# รายงานการตรวจสอบระบบ RPA (Robotic Process Automation) สำหรับ Dynamics 365 BC

เอกสารฉบับนี้อธิบายถึงปัญหาที่พบในสถาปัตยกรรม RPA ปัจจุบันสำหรับการสร้าง Sales Quote อัตโนมัติบนเครื่อง Client (หน้าสาขา) และเปรียบเทียบกับแนวทาง Client-Side Agent (เช่น การใช้ Node.js/Puppeteer) เพื่อประกอบการตัดสินใจ

---

## 1. สถาปัตยกรรมปัจจุบัน: Server-Side RPA
ระบบปัจจุบันออกแบบให้ **Server เป็นคนสั่งงาน Chrome บนเครื่อง Client** ผ่านพอร์ต Remote Debugging (`9222`)

### ❌ ปัญหาที่พบในการออกแบบนี้

1. **ไฟล์ `start_chrome_debug.bat` รันไม่สำเร็จ:**
   - **สาเหตุ:** การกำหนด Path ของ Chrome แบบตายตัว (`C:\Program Files\Google\Chrome\Application\chrome.exe`) อาจไม่ถูกต้องเสมอไป เพราะบางเครื่องอาจติดตั้งไว้ที่ `Program Files (x86)` หรือ User `AppData`
   - **ผลกระทบ:** เมื่อรันไฟล์ Batch จึงไม่มีอะไรเกิดขึ้น Chrome ไม่เปิด และไม่มีการแจ้งเตือน Error

2. **การ Hardcode IP Address ใน Frontend:**
   - ในไฟล์ `frontend/src/pages/CreateQuote/Step6_Summary.jsx` มีการกำหนด IP ของ Client แบบตายตัว:
     ```javascript
     const isLocalDev = window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1';
     const clientIP = isLocalDev ? "127.0.0.1" : "192.192.0.162";
     ```
   - **ผลกระทบ:** หากระบบถูกนำไปใช้ที่สาขาอื่นที่มี IP Address แตกต่างจาก `192.192.0.162` Server จะยังคงพยายามส่งคำสั่งไปที่ IP นี้ ทำให้ไม่สามารถเชื่อมต่อกับ Chrome ของ Client จริงๆ ได้

3. **ปัญหาการบล็อกพอร์ต (Firewall):**
   - การเปิด Remote Debugging Port `9222` จาก Client หมายความว่า Server ต้องเชื่อมต่อเข้ามา (Inbound Connection)
   - **ผลกระทบ:** ตามค่าเริ่มต้น Windows Firewall บนเครื่อง Client มักจะบล็อก Inbound Traffic ส่งผลให้ Server เชื่อมต่อ Chrome ไม่ได้แม้จะระบุ IP ถูกต้องก็ตาม

4. **Missing Dependencies บน Server:**
   - ไฟล์ `backend/requirements.txt` ขาดแพ็กเกจ `selenium`
   - **ผลกระทบ:** เมื่อ Backend พยายามรันสคริปต์ `rpa_create_quote.py` จะเกิด Error ทันที เพราะไม่มีไลบรารีสำหรับควบคุม Browser

---

## 2. การเปรียบเทียบแนวทางแก้ไข (Solutions)

เพื่อให้ระบบสามารถทำงานได้เสถียรและปลอดภัย มี 2 แนวทางหลักที่สามารถเลือกใช้ได้:

### แนวทางที่ 1: ปรับปรุงสถาปัตยกรรมเดิมให้สมบูรณ์ (Server-Side RPA)
ยังคงให้ Server สั่งงาน Chrome ที่เครื่อง Client เหมือนเดิม แต่แก้ไขจุดบกพร่อง

- **วิธีทำ:**
  1. แก้ไขไฟล์ `start_chrome_debug.bat` ให้ค้นหาตำแหน่งของ `chrome.exe` แบบอัตโนมัติ และเพิ่มคำสั่งเปิดพอร์ต `9222` ใน Windows Firewall ชั่วคราว
  2. แก้ไข Logic ใน Frontend หรือ Backend ให้สามารถจับ IP Address ของ Client (เช่น ผ่าน `request.client.host` ใน FastAPI) แบบไดนามิก แทนการ Hardcode
  3. เพิ่ม `selenium` ใน `requirements.txt`
- **ข้อดี:** ไม่ต้องเปลี่ยนโครงสร้างหลักของระบบ, เขียนด้วย Python (Selenium) เหมือนเดิม
- **ข้อเสีย:** เสี่ยงต่อปัญหา Network/Firewall หากเครื่อง Client ไม่อนุญาตให้เชื่อมต่อ Inbound, หรือหาก IP ของเครื่อง Client เปลี่ยนไปมา (DHCP) จะเชื่อมต่อยาก

### แนวทางที่ 2: เปลี่ยนเป็น Client-Side RPA Agent (คล้าย DBD Bridge Server)
แนวทางนี้คือการ **นำโค้ด RPA ไปรันบนเครื่อง Client เอง** แทนที่จะให้ Server สั่งเข้ามา (คล้ายกับที่คุณใช้ Node.js + Puppeteer)

- **วิธีทำ:**
  1. สร้าง Agent เล็กๆ (อาจเขียนด้วย Python, Node.js หรือคอมไพล์เป็น `.exe`) ให้รันอยู่บนเครื่อง Client
  2. เมื่อ User กด "ส่งเข้า BC" หน้าเว็บจะยิงคำสั่งไปหา Agent ตัวนี้ (รันที่ `localhost` ของเครื่องนั้นๆ)
  3. Agent จะเปิด Chrome และรัน RPA Script บนเครื่องตัวเอง
- **ข้อดี:**
  - **หมดปัญหา Firewall:** Agent เชื่อมต่อ Chrome ผ่าน `127.0.0.1` ภายในเครื่องตัวเอง (Localhost) ไม่ต้องกังวลเรื่องการเปิดพอร์ตรับจากภายนอก
  - **ไม่ต้องสนใจ IP:** ไม่ต้องเขียนโค้ดเพื่อหาว่า Client IP คืออะไร
  - มีความเสถียรกว่ามาก เพราะทำงานเสร็จในเครื่องเดียว
- **ข้อเสีย:** ต้องมีกระบวนการ Deploy (แจกจ่าย) ตัว Agent ให้เครื่อง Client ทุกเครื่อง (อาจแจกเป็นไฟล์ ZIP หรือ `.exe`)

---

## 3. สรุปคำแนะนำ

วิธีการสร้าง **Client-Side RPA Agent** (แบบเดียวกับ DBD Bridge Server ที่คุณยกตัวอย่างมา) **เป็นแนวทางที่เหมาะสมและถูกต้องตามหลักวิศวกรรมมากกว่า** สำหรับกรณีที่ต้องควบคุมเบราว์เซอร์บนเครื่อง Client

หากต้องการดำเนินโครงการต่อ ขอให้ตัดสินใจเลือกแนวทางใดแนวทางหนึ่ง:
1. **เลือกแนวทางที่ 1:** หากต้องการความรวดเร็วในการแก้โค้ดที่มีอยู่แล้วให้ทำงานได้ (อาจต้องเซ็ตอัพ Network/Firewall เพิ่มเติม)
2. **เลือกแนวทางที่ 2:** หากต้องการความเสถียรในระยะยาว และพร้อมที่จะปรับเปลี่ยนโครงสร้างไปใช้ Local Agent รันบน Client

กรุณาแจ้งให้ทราบเมื่อเพื่อนของคุณตัดสินใจได้แล้วว่าต้องการพัฒนาไปในทิศทางใด